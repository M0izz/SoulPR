MONAD_RPC_URL=https://testnet-rpc.monad.xyz
# Private key of the wallet that deploys the contract (needs testnet MON for gas)
DEPLOYER_PRIVATE_KEY=
# Wallet address that will be set as backendMinter in the contract constructor
# This is the backend service wallet — its private key lives in backend/.env
BACKEND_MINTER_ADDRESS=
